node_modules/
.tools/
dist/*
!dist/index.html
!dist/og-image.png
perf/output/
perf/index.html

# Claude Code scratch (transient, never committed)
.claude-scratch/

# doclab build products (see docs/review-download-parity/doc/). Anchored with a
# leading slash: a bare `assets/` would also swallow the tracked src/assets/ logo.
/output/
/assets/
# Mermaid's intermediate SVG, emitted beside its source before doclab prepares it.
docs/**/diagrams/*.svg

# aider tool artifacts
.aider*

# e2e failure artifacts (screenshots + console logs)
test/e2e/artifacts/
